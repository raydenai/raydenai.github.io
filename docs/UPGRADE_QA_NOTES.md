# Upgrade QA Notes

## 2026-08-18 — Visual direction prototype review

The direction index at `/directions/` renders all three selectable concepts with clearly differentiated color fields and route links. Its central proposition is visible in the first viewport, although the inherited demo header and announcement bar remain visible; that is acceptable for an internal concept review page but should be replaced by the selected brand’s own navigation in production.

The Authority Film prototype at `/directions/authority-film/` renders successfully. Its hero is materially stronger than the original demo’s split cut-out treatment: a genuine stage image fills the frame, typography occupies a deliberate left-side canvas, and the person reads as operating in a credible public arena. The narrative progression, proof ribbon, visual system, and closing CTA are present below the fold. The internal concept retains the generic demo header; the chosen production direction should receive a direction-specific global shell.

The Proof Machine prototype at `/directions/proof-machine/` renders successfully and establishes a genuinely different information hierarchy: the numerical statement and the receipt grid lead, while the environmental portrait becomes contextual proof instead of the hero asset. The first viewport has substantial whitespace and a hard editorial grid, which suits analytical consultants and operators. It should only be used with real, permissioned figures.

The Idea Studio prototype at `/directions/idea-studio/` renders successfully. It provides the strongest alternative to generic executive-brand composition: an editorial title-first hero, a secondary seated portrait, direct buyer-language cards, and a warm tonal system. It is suitable for a writer, strategist, author, or advisor whose intellectual property is central to the offer. As with the other concepts, the current inherited header is only a review-shell artifact and must be rebuilt once a direction is selected.
